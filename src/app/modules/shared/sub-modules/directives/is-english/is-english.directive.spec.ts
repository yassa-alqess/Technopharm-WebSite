import { IsEnglishDirective } from './is-english.directive';

describe('IsEnglishDirective', () => {
  it('should create an instance', () => {
    const directive = new IsEnglishDirective();
    expect(directive).toBeTruthy();
  });
});
